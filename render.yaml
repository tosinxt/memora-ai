services:
  - type: web
    name: memora-ai
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: waitress-serve --port=$PORT app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0
    plan: free
